---
title: Card
description: "A content container with header, content, and footer sections."
---

<Component path="card-demo" />

## Install

```bash
npx shadcn@latest add @ui-lib/card
```

## Usage

```tsx
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from '@/components/ui/card';
```

## API reference

### Styling

`Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter` accept `style` (`StyleXStyles`, merged last so caller overrides always win) plus all native props of the element they render.

Compose the full anatomy: `CardHeader` (with `CardTitle` + `CardDescription`), `CardContent`, `CardFooter`.
