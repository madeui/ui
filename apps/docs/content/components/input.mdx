---
title: Input
description: "A text input field. Built on Base UI Input."
---

<Component path="input-demo" />

## Install

```bash
npx shadcn@latest add @ui-lib/input
```

## Usage

```tsx
import { Input } from '@/components/ui/input';

<Input placeholder="Email" type="email" />
```

## API reference

### Input

<TypeTable
  type={{
    'disabled': { type: 'boolean', default: 'false' },
    'style': { type: 'StyleXStyles', description: "StyleX styles merged last — they always win over the component's own styles." },
  }}
/>

All native `<input>` props (`type`, `placeholder`, `value`, `onChange`, ...) are forwarded.
