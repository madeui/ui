---
title: Toast
description: "A brief notification. Built on Base UI Toast."
---

<Component path="toast-demo" />

## Install

```bash
npx shadcn@latest add @ui-lib/toast
```

## Usage

```tsx
import { Toaster, ToastProvider, useToast } from '@/components/ui/toast';

// 1. Wrap your app in <ToastProvider> and mount <Toaster /> once.
// 2. In any component:
const toast = useToast();
toast.add({ title: 'Saved', description: 'Your changes were saved.' });
```

## API reference

### useToast().add(options)

<TypeTable
  type={{
    'title': { type: 'string', description: 'Toast heading.' },
    'description': { type: 'string', description: 'Supporting text.' },
    'timeout': { type: 'number', description: 'Auto-dismiss delay in ms (Base UI default: 5000).' },
  }}
/>
