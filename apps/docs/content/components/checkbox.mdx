---
title: Checkbox
description: "A control for toggling between checked states. Built on Base UI Checkbox."
---

<Component path="checkbox-demo" />

## States

<Component path="checkbox-states" />

## Install

```bash
npx shadcn@latest add @ui-lib/checkbox
```

## Usage

```tsx
import { Checkbox } from '@/components/ui/checkbox';

<label>
  <Checkbox defaultChecked /> Accept terms
</label>
```

## API reference

### Checkbox

<TypeTable
  type={{
    'checked': { type: 'boolean', description: 'Controlled checked state.' },
    'defaultChecked': { type: 'boolean', default: 'false' },
    'onCheckedChange': { type: '(checked: boolean) => void' },
    'indeterminate': { type: 'boolean', default: 'false' },
    'disabled': { type: 'boolean', default: 'false' },
    'style': { type: 'StyleXStyles', description: "StyleX styles merged last — they always win over the component's own styles." },
  }}
/>
