---
title: Aspect Ratio
description: "Displays content within a desired ratio."
---

<Component path="aspect-ratio-demo" />

## Install

<Tabs hash="false">
  <Tab title="madeui">

```bash
npx @madeui/cli add aspect-ratio
```

  </Tab>
  <Tab title="shadcn">

```bash
npx shadcn@latest add @madeui/aspect-ratio
```

  </Tab>
</Tabs>

## Usage

```tsx
import { AspectRatio } from '@/components/ui/aspect-ratio';
```

## Square

<Component path="aspect-ratio-square" />

## Portrait

<Component path="aspect-ratio-portrait" />

## API reference

A plain `<div>` using the CSS `aspect-ratio` property.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `ratio` | `number` | — | Width / height, e.g. `16 / 9`. |
| `style` | `StyleXStyles` | — | StyleX styles merged last — always win over the component's own styles. |
