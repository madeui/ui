node_modules/
dist/
.next/
.blume/
.turbo/
*.log
.DS_Store
.env*
!.env.example
.playwright-mcp/

# Screenshots / VRT images — never in git
*.png

# Internal working docs — local only
ROADMAP.md
CLAUDE.local.md
.blume-verify/

# design-sync (claude.ai/design) — local only, never committed:
# .design-sync/ holds a personal claude.ai/design projectId + sync artifacts.
.ds-sync/
ds-bundle/
.design-sync/
